# Engineering Log — GameVault

Keep this updated with real dates/entries as you actually build and commit. A few short entries
spread across the project period, not one entry written retroactively the night before it's due.

## Entry template (copy per session)

**Date:**
**What I worked on:**
**Decisions made and why:**
**Problems hit and how I solved them:**
**Commit(s) from this session:**

---

## Custom requirement note

Bootstrap carousel (featured section) — implemented in `js/carousel.js`. The `FeaturedCarousel`
class fetches featured games from the RAWG API and builds Bootstrap's carousel DOM structure
dynamically, wrapped in a CRT-bezel frame as the page's signature visual element. See the code
comment at the top of that file for the same explanation in-repo.
