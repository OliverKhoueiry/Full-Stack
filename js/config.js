// ---------------------------------------------------------
// GameVault — configuration
// Get a FREE key at https://rawg.io/apidocs (takes ~1 minute)
// then paste it below. Do NOT commit a real key to a public
// repo if you can avoid it — for this class project it's fine
// since RAWG's free tier keys are meant for client-side demos,
// but mention this tradeoff in your README if asked.
// ---------------------------------------------------------
const GV_CONFIG = {
  RAWG_API_KEY: "7af822651bdf467094f11824b4425fd8",
  RAWG_BASE_URL: "https://api.rawg.io/api",
};
